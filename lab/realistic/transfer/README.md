# transfer

실제 고객사 흐름을 모사하기 위한 결과 이동 영역입니다.

- `inbox/`: 대상 서버, DBMS, 장비에서 생성한 결과 파일을 복사하는 위치
- `out/`: Windows 작업 PC에서 `kcii-audit classify-file` 또는 `classify-paste`를 실행해 만든 산출물 위치

이 디렉터리에 실제 고객 데이터, 비밀번호, 토큰, 인증서, 운영 설정 원문을 커밋하지 않습니다.
